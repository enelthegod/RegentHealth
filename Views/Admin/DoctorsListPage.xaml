<Page x:Class="RegentHealth.Views.Admin.DoctorsListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Doctors List">
    <Page.Resources>

        <Style x:Key="BackArrow" TargetType="Button">
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="FontSize"        Value="22"/>
            <Setter Property="Foreground"      Value="#444"/>
            <Setter Property="Padding"         Value="0"/>
            <Setter Property="Width"           Value="36"/>
            <Setter Property="Height"          Value="36"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="Transparent"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
            
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border"
                                        Property="Background"
                                        Value="#E0E0E0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Page.Resources>



    <Grid Margin="40" Background="#f0f0f0">

        <Button Style="{StaticResource BackArrow}"
                Content="&#x2190;"
                ToolTip="Back"
                Click="Back_Click"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="0,0,0,10"/>



        <StackPanel>

            <TextBlock Text="Doctors"
                       FontSize="28"
                       FontWeight="Bold"
                       Margin="0,0,0,20"/>

            <ListBox x:Name="DoctorsList"
                     MouseDoubleClick="Doctor_Open">

                <ListBox.ItemTemplate>
                    <DataTemplate>

                        <Border Background="White"
                                Padding="12"
                                Margin="5"
                                CornerRadius="6"
                                BorderBrush="#DDD"
                                BorderThickness="1">

                            <Grid>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Doctor name -->
                                <TextBlock FontSize="16"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center"
                                           Text="{Binding FullName}"/>

                                <!-- Delete button -->
                                <Button Grid.Column="1"
                                        Content="Delete"
                                        Margin="10,0"
                                        Padding="10,4"
                                        Click="DeleteDoctor_Click"/>

                            </Grid>

                        </Border>

                    </DataTemplate>
                </ListBox.ItemTemplate>

            </ListBox>

            <StackPanel Orientation="Horizontal"
                        Margin="0,20,0,0">

                <Button Content="Create Doctor"
                        Width="150"
                        Height="35"
                        Margin="0,0,10,0"
                        Click="CreateDoctor_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>