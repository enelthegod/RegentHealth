<Page x:Class="RegentHealth.Views.Admin.TodayAppointmentsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Today Appointments">

    <Grid Margin="40" Background="#f0f0f0">

        <StackPanel>

            <Button Click="Back_Click"
            Width="40"
            Height="40"
            Background="Transparent"
            BorderThickness="0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Margin="10"
            Cursor="Hand">

                <TextBlock Text="←"
                   FontSize="20"
                   FontWeight="Bold"/>
            </Button>

            <!-- Page title -->
            <TextBlock Text="Today's Appointments"
                       FontSize="28"
                       FontWeight="Bold"
                       Margin="0,0,0,20"/>

            <ListView x:Name="AppointmentsList">

                <!-- Handle double-click on item -->
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <EventSetter Event="MouseDoubleClick" Handler="Appointment_DoubleClick"/>
                    </Style>
                </ListView.ItemContainerStyle>

                <!-- Grouping by doctor -->
                <ListView.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate>
                                <!-- Display doctor name with prefix -->
                                <TextBlock Text="{Binding Name, StringFormat=Doctor: {0}}"
                                           FontSize="18"
                                           FontWeight="Bold"
                                           Margin="0,10,0,5"/>
                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>
                    </GroupStyle>
                </ListView.GroupStyle>

                <!-- Template for each appointment -->
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="8"
                                Margin="4"
                                Background="White"
                                BorderBrush="#DDD"
                                BorderThickness="1"
                                CornerRadius="6">
                            <StackPanel Orientation="Horizontal">
                                <!-- Appointment time -->
                                <TextBlock Width="60"
                                           Text="{Binding AppointmentDate, StringFormat='{}{0:HH:mm}'}"/>
                                <!-- Patient name -->
                                <TextBlock Width="160"
                                           Text="{Binding PatientFullName}"/>
                                <!-- Appointment status -->
                                <TextBlock Text="{Binding Status}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>